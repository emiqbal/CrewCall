
<div style="margin:1% 5%;">
  <h2>Please Authenticate with DocuSign</h2>
  <p class="lead">
    <form class="eg" action="/auth/docusign" method="POST"  data-busy="form">
     <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
      <div class="form-group form-inputs type-form-control mb-0">
        <select id="auth" name="auth" class="form-control">
          <option value="grand-auth" selected>Authorization Code grant</option>
        </select><br>
      </div>
        <button class="btn btn-flat w-100">Authenticate with DocuSign</button>
    </form>
  </p>
  <hr class="my-4">
  <p>You need to authenticate with DocuSign to sign your crew contract.</p>
</div>
