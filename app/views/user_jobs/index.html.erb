<h1>All My Jobs Application</h1>

<%= UserJobsCalendar.new(self, events: @calendar_events).render do |date, user_jobs| %>
  <%= date.day %>
<% end %>
<%# month_calendar(events: @user_jobs) do |date, user_jobs| %>
  <%# date.day %>

  <%# user_jobs.each do |user_job| %>
  <%#end %>
<%#end %>

<% @notifications.each do |notification| %>
  <div class="alert alert-warning" role="alert">
    <p><%= render notification %></p>
  </div>
<% end %>


    <h3>Applied</h3>
     <% @applied.each do | user_job | %>
      <div class="submissions">
        <%= link_to project_path(user_job.job.project) do%>
        <h5><%= user_job.job.project.title %></h5>
        <% end %>
        <p>Job Title: <%= user_job.job.title %> </p>
        <p>Job Dates: <%= "#{user_job.job.start_date.strftime('%d %b %Y')} to #{user_job.job.end_date.strftime('%d %b %Y')}" %></p>
        <p>Job Status: <%= user_job.status %> </p>
      </div>
    <% end %>

    <h3>Approved</h3>
    <% @approved.each do | user_job | %>
      <div class="submissions">
        <%= link_to project_path(user_job.job.project) do%>
        <h5><%= user_job.job.project.title %></h5>
        <% end %>
        <p>Job Title: <%= user_job.job.title %> </p>
        <p>Job Dates: <%= "#{user_job.job.start_date.strftime('%d %b %Y')} to #{user_job.job.end_date.strftime('%d %b %Y')}" %></p>
        <p>Job Status: <%= user_job.status %> </p>
        <%= link_to "Go to Project Acceptance Page", job_user_job_path(user_job.job, user_job), class: 'btn btn-flat'  %>
      </div>
      <% end %>

    <h3>Confirmed</h3>
     <% @confirmed.each do | user_job | %>
      <div class="submissions">
        <%= link_to project_path(user_job.job.project) do%>
        <h5><%= user_job.job.project.title %></h5>
        <% end %>
        <p>Job Title: <%= user_job.job.title %> </p>
        <p>Job Dates: <%= "#{user_job.job.start_date.strftime('%d %b %Y')} to #{user_job.job.end_date.strftime('%d %b %Y')}" %></p>
        <p>Job Status: <%= user_job.status %> </p>
      </div>
      <% end %>

    <h3>Rejected</h3>
     <% @rejected.each do | user_job | %>
      <div class="submissions">
        <%= link_to project_path(user_job.job.project) do%>
        <h5><%= user_job.job.project.title %></h5>
        <% end %>
        <p>Job Title: <%= user_job.job.title %> </p>
        <p>Job Dates: <%= "#{user_job.job.start_date.strftime('%d %b %Y')} to #{user_job.job.end_date.strftime('%d %b %Y')}" %></p>
        <p>Job Status: <%= user_job.status %> </p>
      </div>
      <% end %>

<%= link_to "Back", :back, class: 'btn btn-flat'  %>
<%= link_to "Go to All Projects", root_path, class: 'btn btn-flat'  %>
